'use client';
import { VenetianMask } from 'lucide-react';

const AppLogo = () => {
  return (
    <div className="flex items-center justify-center p-1 rounded-sm bg-primary">
      <VenetianMask color="white" size={26} />
    </div>
  );
};

export default AppLogo;
